import { Fragment, useRef } from "react";

function Livestream(video, width, height) {
  // new Twitch.Player("twitch-embed", { channel: "whoalookout" });
  return <div id="twitch-embed"></div>;
}

export default Livestream;
