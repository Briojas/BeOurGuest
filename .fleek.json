{
  "build": {
    "image": "fleek/next-js",
    "command": "cd GUI && yarn && yarn build && yarn export",
    "publicDir": "out",
    "baseDir": "",
    "environment": {
      "SOME_ENV": "Build finished!"
    }
  }
}
